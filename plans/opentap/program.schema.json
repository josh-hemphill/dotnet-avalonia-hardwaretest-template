{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hardwaretest.local/program.schema.json",
  "title": "HardwareTest program sidecar",
  "description": "Session, DUT, and Typst report policy beside a locked .TapPlan. Instrument requirements belong in .TapPackage Dependencies, not here.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Optional pointer to this schema for editor intellisense."
    },
    "displayName": { "type": "string" },
    "dutFamily": { "type": "string" },
    "requireSerial": { "type": "boolean" },
    "requirePartNumber": { "type": "boolean" },
    "requireRevision": { "type": "boolean" },
    "requireOperator": { "type": "boolean" },
    "reportKinds": {
      "type": "array",
      "minItems": 1,
      "items": { "enum": ["status", "certification"] },
      "description": "Typst PDFs to generate after a run."
    },
    "defaultReportKind": {
      "enum": ["status", "certification"],
      "description": "Kind Results opens on double-click. Must be listed in reportKinds."
    },
    "selectionIncludesCleanup": {
      "type": "boolean",
      "description": "When false, Run Selected excludes SafeShutdownStep."
    }
  }
}
